# Current Task

No active implementation task.

The 2026-07-14 Workflow adjustment adds a read-only `Workflow` tab that derives the current route from the authoritative running job or, when idle, the visible workspace settings. It explains why the flow is shown, separates project context, routing, agent work, review, commit, integration and push into seven explicit stages, and classifies Git, Graphify, Obsidian, cli-continues, providers and configured MCP tools as active, ready, on demand, unused or unavailable. The projection exposes no task body, logs, local paths, MCP targets or credential values and performs no workflow mutation. Its responsive grid is designed to use one seven-stage row at 2550 pixels and to wrap whole cards at narrower widths.

The 2026-07-14 integration audit reviews all 26 catalog systems, seven enabled Codex plugins and the current skill/MCP surfaces. Serena and Codex-managed `node_repl` retain their distinct structured roles; deterministic tools remain CLI/runtime integrations and project-dependent media/image/game tools stay capability-gated. The official OpenAI Developer Docs MCP is now project-scoped in `.codex/config.toml`, restricted to its five advertised read-only tools, optional at startup, credential-free and outside API billing. Plugin-managed and cached connector surfaces are not duplicated as classic MCP entries. CC Switch and Comfy Cloud are documented as optional removal/disable candidates only when their external workflows are unused; nothing was uninstalled automatically.

The same audit removed the existing Codex CLI false negative: installation and component checks now use the npm-provided `codex.cmd` shim instead of the blocked Windows App Execution Alias `codex.exe`.

The 2026-07-14 Systems adjustment replaces the primary `Systeme` surface with a read-only `MCP-Server` view. It combines global and selected-project Codex and Claude Code configurations, distinguishes local STDIO from remote HTTP, exposes scope and the integration contract, and reports configuration completeness without starting arbitrary servers or claiming a live connection. URL credentials and query values plus environment, argument and header values stay out of the API; the UI shows only safe start targets and referenced variable/header names. Existing installers, updates and custom local-system registration remain available under the secondary `Lokale Laufzeiten & Diagnose` disclosure.

The 2026-07-14 Knowledge adjustment contains the Graphify node list in a dedicated keyboard-focusable vertical scrollbar. Its height follows the graph at desktop, tablet and mobile breakpoints, while the node-list heading and selection guidance stay visible and node/relationship selection behavior remains unchanged.

The 2026-07-13 model-selection, execution-UX, live-progress, Graphify interaction, Git-review alignment and desktop-launcher hardening is implemented in the current working tree and awaits owner Git review. It adds a versioned server-owned model catalog, deterministic task profiles, strict pre-execution model validation, exact Ollama/Hermes model routing, visible provider/model audit data, project-safe refresh handling and explicit loading, empty, unavailable, stale and running states. The execution panel now uses progressive disclosure, clearer action semantics and responsive layouts without horizontal page overflow; its mode help is a compact right-aligned indicator with a full-size accessible target. In the two-column workspace, execution and chat share a dynamic viewport height, overflowing execution controls scroll locally and the 2000-plus-pixel layout expands while keeping the chat dominant. Graphify canvas, node list and direct-relationship controls now share one selection state with centering, synchronized highlighting and clickable related nodes. The Git review commit field and available actions share one aligned 44-pixel row on desktop and wrap into balanced rows at narrower breakpoints. Live jobs remain visible over their incomplete run directories and show current activity plus timestamps; interrupted and orphaned runs can no longer masquerade as active work. The desktop shortcut uses the dedicated restart entry point so it replaces an already running dashboard service with the current repository version before opening the browser.

Validation completed:

- `npm test`: PASS, including 57 Node tests with current-workflow derivation and API redaction, Router isolation and product smoke coverage.
- Workflow responsive browser QA: PASS at 375, 768, 1024, 1920 and 2550×1440 pixels with seven stages and eleven classified tools, no horizontal overflow, 44-pixel minimum visible button targets, keyboard tab navigation, explicit refresh loading feedback and no browser console errors. At 2550×1440 all seven 293-pixel stage cards form one row inside the 2,140-pixel panel; at 375 pixels the stages and tool cards form readable single-column rows.
- MCP inventory validation: PASS for read-only API behavior, Codex TOML and Claude JSON project/global parsing, current-project isolation, URL redaction and exclusion of credential values.
- MCP integration audit: PASS for project-config discovery, multiline five-tool allow-list parsing, official server initialization, advertised-tool verification and one end-to-end read-only documentation search.
- Installed-tool live audit: PASS for all 26 catalog rows; Codex CLI now resolves as installed through `codex.cmd`.
- MCP responsive browser QA: PASS at 375, 768, 1024, 1920 and 2550×1440 pixels with no horizontal overflow, 44-pixel minimum interactive targets, adaptive 2/4-column metrics, responsive server cards, keyboard-operable integration disclosures and no browser console errors. At 2550×1440, all four server cards form one balanced row at 544 pixels each with 10-pixel gaps inside the 2,238-pixel panel; the expanded five-tool policy remains fully visible.
- Targeted API boundaries: PASS, including rejection of an invented active model before provider execution.
- Local Ollama capability check: PASS; six installed Completion models were executable and the installed embedding-only model was rejected with a specific reason.
- `git diff --check`: PASS.
- Knowledge node-list validation: PASS for semantic focusable-region markup, shared 620/440/360-pixel graph breakpoints, contained vertical overflow and the complete automated test suite. In-app visual QA was unavailable for this adjustment because the installed Browser plugin is missing its bundled `scripts/browser-client.mjs` runtime file.
- In-app browser QA: PASS at 375, 768, 1024, 1920 and 2550×1440 pixels with no horizontal page overflow, no sub-44-pixel interactive targets and no browser console errors. At 2550×1440, execution and chat both measure 1282 pixels high, execution grows to 440 pixels, chat retains 1680 pixels and execution overflow scrolls without moving the page; the mode helper renders as a 20-pixel glyph inside its 44-pixel target. Git commit input and actions share identical top and bottom edges from 1024 pixels upward, then wrap to two columns at 768 pixels and one column at 375 pixels.
- Interactive browser QA: PASS for profile changes, an exact model override and reset, catalog/model decision details, loading-to-ready task gating, the keyboard-visible skip link and bidirectional Graphify selection between canvas, node list and direct relationships.
- Desktop restart shortcut: PASS; the previous dashboard PID was stopped, a different PID became healthy on port 8765 and the shortcut target was verified.
- Live-progress recovery: PASS; an interrupted real job is rendered as failed instead of `EXTERNAL`, current activity is exposed through a polite live region and incomplete run/job reconciliation is covered by behavior tests.

Next owner action: inspect the working-tree diff in `Prüfen & Git` and separately authorize any commit, integration or push action.
